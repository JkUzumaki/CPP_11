/*
1)
	Before C++11 vectors needs to be initialized like below,
		vector<int> v;
		v.push_back(3);
		v.push_back(2);
		v.push_back(4);
		v.push_back(5);
	But in c++11 we can initialize them as follow
		vector<int> v = {3, 4, 1, 9};
-----------------------------------------------------------------------
2)
	vector, list etc
	can be assigned the value directly without assignment operator
	
-----------------------------------------------------------------------\
3)
	overloading a parameter to an object
		
*/
